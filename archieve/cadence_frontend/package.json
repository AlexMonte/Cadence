{
  "name": "cadence-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "./scripts/dx.sh serve --web --cross-origin-policy",
    "build": "./scripts/dx.sh bundle --web --out-dir dist",
    "preview": "./scripts/dx.sh serve --web --release --cross-origin-policy",
    "test:e2e": "npx @playwright/test test --config=playwright.config.mjs",
    "test:e2e:headed": "npx @playwright/test test --config=playwright.config.mjs --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "vite": "^6.1.1"
  },
  "dependencies": {
    "@codemirror/state": "^6.5.4",
    "@codemirror/view": "^6.39.16"
  }
}
